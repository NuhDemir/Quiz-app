# MongoDB connection string
MONGODB_URI=mongodb+srv://nuhdemirdev_db_user:8Q1dfuBKnncJdYJy@cluster0.oaip2hf.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# Authentication
JWT_SECRET=super-secret-key-change-me
# Optional: override default 7 day expiration (e.g. "30d")
# JWT_EXPIRES_IN=7d

# Bcrypt salt rounds (optional override)
# BCRYPT_SALT_ROUNDS=10


# Rate limiting (future)
# RATE_LIMIT_WINDOW_MS=60000
# RATE_LIMIT_MAX=100

# Application base URL (frontend adresi - Netlify deploy URL veya local)
APP_URL=http://localhost:8888

## Email verification & password reset feature kaldırıldı, ilgili email provider değişkenleri artık gerekmiyor.
